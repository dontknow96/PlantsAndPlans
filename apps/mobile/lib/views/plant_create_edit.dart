import 'package:flutter/material.dart';

class PlantCreateEdit extends StatelessWidget {
  const PlantCreateEdit({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
